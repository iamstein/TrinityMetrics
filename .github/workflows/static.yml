# Disabled in favor of .github/workflows/publish.yml, which builds and deploys the rendered Quarto site.
name: Disabled static Pages workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Static workflow disabled
        run: echo "This workflow is disabled. Use publish.yml for GitHub Pages deployment."
